== Simple model classes for hello world example 

Usage: 

* Make sure Sqlite3 is installed
* execute `mvn jetty:run`
* Open http://localhost:8080/Helloworld 
* Look at the awesome list of users in the database
